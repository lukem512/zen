extends layout

block content
    h1= title
    form#formAddGroup(name="addgroup",method="post",action="/admin/groups/new")
        input#inputGroupName(type="text", placeholder="Name", name="name")
        textarea#inputGroupDescription(placeholder="Optional description about the group", name="description")
        button#btnSubmit(type="submit") Create